#ifndef LOADED_RESOURCE_HH
#define LOADED_RESOURCE_HH

#include <map>
#include <vector>

namespace objects {

    // LOADED TEXTURES
    // extern std::map<objects::MODELNAME, unsigned int> textureMap;

    // void createGlTexture(objects::MODELNAME _modelname, std::vector<unsigned char> _obj_textureDataBuffer, unsigned int& obj_imgWidth, unsigned int& obj_imgHeight);

}



#endif