#ifndef OBJECT__TYPES_HH
#define OBJECT__TYPES_HH

namespace objects {


// MODELS AS STRINGS : e.g. "resources/models/triangle.pso" or "house-1"
typedef enum MODELNAME {
    blend_cube_texture_1 = 10,
} MODELNAME;

// list compatible shaders for each model [This might change as each model might have multiple data-files, and what I support in my own rendering pipeline]


}

#endif